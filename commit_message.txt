phase 16: soften patient voice and add llm trace logging

Problem:
Patient-side replies were still sounding too flat, repetitive, and rigid in the live LLM path, especially on confirmation, wrap-up, insurance, DOB-correction, and human-handoff turns.

Change:
- Softered the patient prompt language in scenarios.py while keeping identity/DOB/fact grounding and turn-taking guardrails intact.
- Added scenario-specific voice guidance for the harder edge cases.
- Added reply shaping and variant pools in engine.py so confirmation, goodbye, wrap-up, insurance, DOB correction, and escalation replies sound more natural.
- Wrote live LLM traces to a separate artifacts folder and kept that output out of git.
- Added guardrail and live-LLM coverage in the test suite.
- Added a small retry path for transient 429s so the live smoke is less brittle.

Context:
This work was created for the Loom debug video. I started the flow on Loom, but finishing every implementation detail and validation step on camera was not feasible within the time window, so I completed the remaining work off Loom from the planned breakdowns and then validated the result locally.

Validation:
python -m unittest tests.test_guardrails -q